# 臨時檢查圖片
_check_*.png
_v3*.png
*.jpe

# Python
__pycache__/
*.pyc
.venv/

# 測試
.pytest_cache/

# 生成物（generate_html.py 產生，同 index.html 完全重複；離線版請用 index.html）
空調對比報告.html

# 抓取原子寫入暫存檔
*.csv.tmp

# WSL 環境設定嘅臨時腳本/日誌（唔屬於本專案）
wsl*.ps1
wsl*.sh
wsl*.cmd
wsl*.log
wsl*.txt
enable-wsl*.ps1

# 系統
Thumbs.db
desktop.ini

# API 憑證（PricesAPI key 只放環境變數 / GitHub Secrets）
.env
.env.*

# metadata 兩階段封裝（D14）：core 檔係未完成件，只應存在於 RUNNER_TEMP／本機暫存，唔入庫
metadata.core.json

# ds-exec／AI 工作記錄及整合 worktree（唔係產品內容；防止 git add -A 誤提交）
.agents/

# release 打包標記（由 pack 步驟寫入 tarball，唔入庫）
.release-commit

# 私人部署筆記／憑證（唔進公開 repo）
private-server-notes/
private-deployment/
_site/
